import * as React from "react";
import s from "./Header.module.css";

const Header = () => {
  return (
    <div className={s.header}>
      <section className={s.section__top}>
        вап
      </section>
    </div>
  );
};

export default Header;
